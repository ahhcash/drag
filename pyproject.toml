[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "drag"
version = "0.1.0"
description = "dRAG - RAG over your docs!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.3",
    "crawlee>=0.5.2",
    "fastapi>=0.115.7",
    "html2text>=2024.2.26",
    "httpx>=0.28.1",
    "langchain-community>=0.3.16",
    "langchain>=0.3.16",
    "langdetect>=1.0.9",
    "lxml>=5.3.0",
    "prefect>=3.1.14",
    "requests>=2.32.3",
    "tiktoken>=0.8.0",
    "uvicorn>=0.34.0",
    "langchain-openai>=0.3.2",
    "psycopg[binary,pool]>=3.2.4",
    "sqlmodel>=0.0.22",
    "asyncpg>=0.30.0",
    "pgvector>=0.3.6",
]

[project.optional-dependencies]
dev = [
    "mypy>=1.14.1",
    "ruff>=0.9.3",
    "types-requests",
    "types-beautifulsoup4",
    "build>=1.0.3",
]

[tool.hatch.build.targets.wheel]
packages = ["app"]
